import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Nonaktifkan indikator dev bawaan Next.js agar ikon "N" tidak muncul di pojok layar.
  devIndicators: false,
};

export default nextConfig;
